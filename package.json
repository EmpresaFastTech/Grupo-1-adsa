{
  "name": "institucional-fasttech",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node ./site-institucional/bin/www",
    "postinstall": "cd site-institucional && npm install"
  }
}